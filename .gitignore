.DS_Store
tien-len-deploy.zip
AGENTS.md
CLAUDE.md

# Thư mục tạm / file trung gian tạo ảnh
tmp/

# Cache deploy của Firebase (local, không cần lên git)
.firebase/

# Cấu hình/agent cục bộ của Claude Code
.claude/
